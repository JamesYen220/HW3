import { createBrowserHistory } from 'history';

//manages the page navigation as par the browser preference. the history for the forward and go-back button on the top of the browser
export const history = createBrowserHistory();